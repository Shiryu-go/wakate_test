package jjug.ccc.fall3.wakate_test.Record;

public record ToDo(
        int ListId,
        int id,
        String ToDo_Title,
        String ToDo_Description,
        Boolean Done,
        String deadline
) {}
